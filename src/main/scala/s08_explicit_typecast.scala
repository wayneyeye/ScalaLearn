object s08_explicit_typecast {
  def main(args: Array[String]): Unit = {
    // declare a value/variable without initialization
    // function arguments
    // returned values of a function
      // with return
    def test_f():Int={
      return 1
    }
      // recursive call
      // overloaded methods with reuse
      // if scala inferred as a broader type like Any

  }
}
